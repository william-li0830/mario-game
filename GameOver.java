import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * A Game Over banner that appears when the player loses
 */

public class GameOver extends Actor
{
    // I added the game over sign
    public GameOver()
    {
        getImage().scale(550, 80);
    }
}
